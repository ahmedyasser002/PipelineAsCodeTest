pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build successfully'
            }
        }
        stage('Test'){
            steps{
                echo'Test Completed'
            }
        }
    }
}
